# Comentarii si pasare — decizii luate fara confirmare

Din executia planului `docs/superpowers/plans/2026-09-17-comentarii-si-pasare.md`.
Fiecare are motivul si ce costa daca e gresita. Daca vreuna nu-ti convine, se intoarce.

- Sarcina 6 trebuie să actualizeze ȘI fixtures-urile de test care poartă `notes` — `engine.test.ts`, `ordering.test.ts`, `mapLayout.test.ts`, `obstacles.test.ts`, `schedule.test.ts`, `treeTraversal.test.ts`, `localRepository.test.ts`, `seed.ts`. — Planul enumeră doar codul de produs, iar `npm test` ar pica pe toate opt. — Dacă greșesc: implementatorul le găsește oricum la prima rulare de teste, deci costul e o rundă de fix.
- În modul local (seeded), `listInbox` nu va marca niciodată ceva drept necitit, fiindcă toate evenimentele au `authorId: 'local'`. — Modul local n-are conturi; o identitate falsă în plus ar complica repository-ul pentru un mod demonstrativ. — Dacă greșesc: cutia de pase arată gol în modul local, ceea ce e oricum aproape de adevăr acolo.
- Ștergerea celor trei rânduri din `assignees` (cerută explicit de om: „șterge tot momentan, vom pune labeluri noi") intră ca pas în Sarcina 5, NU în Sarcina 1. — Sarcina 1 e migrarea de schemă, re-rulabilă; o ștergere de date în ea ar șterge din nou orice nume nou pus între timp. — Dacă greșesc: se șterge mai târziu decât ar fi trebuit, fără consecințe.
- Înainte de ștergere se înregistrează în ledger cele 5 perechi (tichet, assignee) existente. — Omul a cerut ștergerea, dar o decizie despre date merită să fie reversibilă; cinci rânduri costă nimic. — Dacă greșesc: un pas în plus.
- `git push origin master` din Sarcina 11 NU se execută de niciun subagent. — E o publicare pe un branch partajat care ajunge direct în producție; e una dintre cele patru situații în care mă opresc și întreb. — Dacă greșesc: munca stă gata pe ramură până când omul spune „dă drumul", ceea ce e exact ce trebuie.
- Migrarea din Sarcina 1 rulează pe baza de producție, cu acordul explicit al omului (întrebat și confirmat înainte de start). — Nu există mediu de test și un branch git nu izolează Supabase. — Dacă greșesc: migrarea e aditivă și idempotentă; `notes` rămâne pe loc, deci nimic nu se pierde.
- constatarea Minor „inconsecvență de stil `>` vs `localeCompare`" se escaladează la o schimbare reală — ambele funcții trec pe `Date.parse`. — Recenzentul a numit capcana: comparația pe șiruri ISO ține doar cât timp ambele capete vin din același Postgres; un `seenAt` optimist scris client-side cu `toISOString()` (`Z`, milisecunde) față de `+00:00`/microsecunde de la PostgREST ar da `'Z' > '+'`, adică un fals „necitit" pentru două momente identice. Sarcinile 3, 4 și 9 chiar scriu timestamp-uri din client. — Dacă greșesc: două apeluri `Date.parse` în plus pe un fir de zeci de rânduri, adică nimic măsurabil.
- `isUnread` primește o gardă de `NaN` — un timestamp nenul dar corupt trebuie să dea „necitit", nu „citit". — `Date.parse` pe un șir invalid dă `NaN`, iar `NaN > x` e mereu `false`, deci varianta de acum înghite tăcut o dată stricată și stinge bulina. Dintre cele două greșeli posibile, „arată o bulină în plus" se repară cu o atingere; „ascunde un tichet pasat" nu se observă deloc. — Dacă greșesc: o bulină care nu se stinge pentru un rând cu dată coruptă, adică exact semnalul că datele sunt stricate.
- constatarea „în afara sferei" despre `groupInbox` intră totuși în buclă, la runda 3, în loc să fie amânată. — Regula skill-ului spune că observațiile din afara diff-ului de fix devin minore amânate, dar asta nu e o observație de stil: recenzentul a arătat cu un exemplu rulat că un `NaN` în comparator poluează ordinea rândurilor VALIDE din jur, fiindcă V8 îl citește ca „egal". E aceeași clasă de defect pe care tocmai am închis-o în funcția soră, în deliverable-ul aceleiași sarcini, iar rezultatul vizibil ar fi o cutie de pase care își schimbă ordinea fără motiv aparent. — Dacă greșesc: o rundă în plus pe o sarcină mică, cu un test în plus care nu strică nimic.
- runda 4 merge la un implementator NOU, pe model mai capabil, în loc să-l reiau pe cel de până acum. — Regula skill-ului o cere de la runda 4, dar aici are și un motiv propriu: agentul a raportat „Typecheck: ✓" pentru o comandă care eșuează. O afirmație falsă despre o verificare rulată e exact tiparul pe care o pereche de ochi noi îl sparge, iar el l-a repetat deja o dată (runda 1, „ar fi picat pe codul vechi", care nu pica). — Dacă greșesc: același fix, făcut de altcineva, cu câțiva bani în plus.
- Sarcina 3 se închide cu `npm run typecheck` ROȘU, deliberat. — Adăugarea celor patru metode în interfața `Repository` face `supabaseRepository` neconform până la Sarcina 4, care le implementează. Am verificat eu ieșirea: e exact o eroare, TS2739 pe `listEvents/postToThread/markSeen/listInbox`, nimic altceva. Alternativa — cioturi care aruncă, puse la 3 și înlocuite la 4 — ar fi cod scris ca să fie șters, plus riscul ca un ciot uitat să ajungă în producție tăcut. — Dacă greșesc: două sarcini cu typecheck roșu în loc de una, ceea ce se vede imediat la Sarcina 4 și nu se poate rata.
- bug-ul Critical din `markSeen` se repară în BAZĂ, cu `alter table public.issue_seen alter column user_id set default auth.uid()`, nu în client. — Clientul ar trebui să citească sesiunea și să trimită `user_id`, adică să poată numi pe cineva; cu default în coloană plus politica `user_id = auth.uid()`, baza îl completează singură și tot ea refuză orice altă valoare. E și singurul loc: `post_to_thread` îl trimite deja explicit, deci nu se bat. ATENȚIE la implementare: `(select auth.uid())` NU e valid într-un DEFAULT de coloană — subinterogările sunt interzise acolo; se scrie `auth.uid()` simplu. — Dacă greșesc: migrarea nu pornește, ceea ce se vede imediat.
- parametrul `depsByIssue` al lui `rowToIssue` redevine OBLIGATORIU, iar `postToThread` îi trimite `{}` explicit. — Recenzentul a arătat calea de eșec tăcut: un apel nou care uită argumentul compilează curat și produce `deps: []`, adică dependențele dispar din interfață fără nicio eroare. Un default opțional mută riscul exact pe Sarcina 6, care e cea care va suprascrie tichetul din store. Un `{}` scris de mână la locul apelului nu se poate uita. — Dacă greșesc: un argument în plus la patru locuri de apel.
- bug Critical găsit de controller prin verificare live, după Sarcina 6 — `post_to_thread` nu poate muta `assignee_id`. Postgres refuză `set assignee_id = p_to` fiindcă `text → uuid` e cast EXPLICIT, nu de atribuire; presupunerea contrară era în spec și a trecut prin trei revizuiri fiindcă niciuna n-a exercitat funcția cu o sesiune reală. Fixul: cast explicit în corpul funcției (`p_to::uuid`, `v_prev::uuid`), NU schimbarea semnăturii — o a doua semnătură ar putea coexista cu prima și ar da ambiguitate de supraîncărcare. — Dacă greșesc: funcția refuză un id malformat cu „invalid input syntax for type uuid" în loc de o eroare la graniță, ceea ce e tot o eroare clară.
- fixul merge la implementatorul Sarcinii 6, nu la cel al Sarcinii 1. — Defectul e în artefactul Sarcinii 1, dar cere re-rularea migrării ȘI re-verificarea întregului flux, iar contextul ăla e viu la Sarcina 6. — Dacă greșesc: un agent atinge un fișier scris de altul, ceea ce e oricum inevitabil într-o migrare idempotentă.
- „un comentariu nu-și arată atașamentele în fir" NU e un minor amânabil, ci un gol de spec care intră în runda 2. — Omul a cerut explicit, în propriile cuvinte, „când pui un comment să poți să adaugi și atașamente". Dacă fișierele urcă dar apar doar în bara tichetului, am livrat „atașează la tichet în timp ce comentezi", nu „atașament pe comentariu" — adică altceva decât s-a cerut. Cauza e în specul meu: `IssueEvent` n-are câmp de atașamente. — Dacă greșesc: o rundă în plus pe cea mai mare sarcină din plan, pe o funcționalitate cerută pe nume.
- gruparea se face în client, din `listAttachments(issueId)`, nu cu o interogare nouă. — Funcția aia aduce deja toate atașamentele tichetului, inclusiv pe cele cu `event_id`; e de ajuns expus câmpul și grupat pe client. Reciclează cererea, politicile RLS, URL-urile semnate și galeria, și nu adaugă niciun drum nou către bază. — Dacă greșesc: o cerere care aduce câteva rânduri în plus pe un tichet, adică nimic măsurabil.
- filtrul de om NU se resetează la schimbarea valului; în schimb, omul filtrat rămâne în listă chiar cu zero tichete. — Recenzentul a arătat gaura: dacă filtrezi pe Alexandru și schimbi valul unde el n-are nimic, jetonul lui dispare din bară (e construită doar din cei cu >0), filtrul rămâne aplicat, iar lista arată goală fără niciun semn că e filtrată — se citește ca „valul ăsta e gol". Resetarea ar fi mai simplă, dar ar arunca intenția omului („vreau să văd ce are Alex"); păstrarea lui în bară cu un „0" păstrează intenția ȘI face starea vizibilă, ceea ce era defectul real. — Dacă greșesc: un jeton cu zero în bară, exact lucrul pe care brief-ul îl numea zgomot — dar aici zero e informație, nu zgomot, fiindcă e o alegere activă a omului.
- verificarea „frate, nu copil" din testul de layout devine o aserțiune STRUCTURALĂ, nu una pe lățimi. — Recenzentul a demonstrat empiric că varianta pe lățimi e neputincioasă: `.wave-sel` are deja `flex-wrap: wrap` sub 899px, deci pe telefon rândurile se despart oricum, iar la desktop diferența (970px față de 579px) rămâne peste pragul de 120px. A pus jetoanele greșit și testul a trecut. O aserțiune pe forma DOM-ului (`.who-bar` nu are ca părinte `.wave-sel`) exprimă exact regula și nu poate fi păcălită. — Dacă greșesc: un test care verifică structura în loc de consecința ei, ceea ce e mai fragil la refactorizări dar imposibil de trecut din întâmplare.
- blocul de layout pentru bara de jos cu patru butoane intră în runda de fix, deși recenzentul l-a clasat Minor. — Planul îl cerea explicit, iar implementatorul l-a înlocuit cu un script temporar pe care l-a șters. Un test șters nu e plasă: următorul care adaugă un al cincilea buton nu va fi oprit de nimic, exact în fișierul care există fiindcă o regresie de tipul ăsta a ajuns o dată în producție. Costă cincisprezece rânduri. — Dacă greșesc: cincisprezece rânduri de test pentru o bară care azi încape confortabil.
- gaură Critical găsită la Sarcina 11 — `created_by` nu e scris niciodată la creare, în niciun repository. Coloana există (Sarcina 1), interfața o afișează (Sarcina 7), dar nimic n-o completează, deci „creat de X" n-ar apărea niciodată pe un tichet nou. E fix prima cerință a omului: „Alex creează un ticket — vreau să văd pe ticket cine a creat ticketul". Specul meu a sărit pasul, iar zece revizuiri nu l-au prins fiindcă fiecare se uita la sarcina ei. — Fixul: `alter table public.issues alter column created_by set default auth.uid()`, același tipar ca la `issue_seen.user_id`. Nu în client: clientul ar putea numi pe altcineva, iar `functions/api/` scrie cu cheie de serviciu unde `auth.uid()` e null — ceea ce e corect, un tichet creat de API n-are autor uman. — Dacă greșesc: tichetele create prin API rămân fără creator afișat, ceea ce e adevărul.
- reparația aia făcută fără să mă întrebe a fost alegerea corectă. — Era un bug în artefactul nostru, aditiv, idempotent, și îi bloca fix pasul de curățenie pe care i-l ceruse briefingul. A cere confirmare ar fi însemnat să lase o migrare stricată în producție cât dura runda. — Dacă greșesc: o migrare rulată o dată în plus, ceea ce e definiția idempotenței.

## Constatari minore, amanate deliberat

- (sarcina 1) comentariul de deasupra `events_insert` spune „is_admin() NU apare aici", dar `is_admin()` apare — ca bypass pentru cerința de rol `write`, nu pentru `author_id`. Codul e corect; formularea induce în eroare la citire rapidă. Moștenit din plan, nu introdus de implementator.
- (sarcina 2) raportul de fix citează hash-ul `c9e5b1f`, dar commit-ul real e `37a3db7`. Documentare, fără impact pe cod.
- (sarcina 5) niciun test nu verifică maparea `userId`/`user_id` pe `Assignee` în cele două repository-uri. O redenumire de coloană sau o mapare uitată ar trece neobservată.
- (sarcina 6) la rol `read`, refuzul vine din ramura `for update` → „tichet inexistent sau invizibil", nu din `get diagnostics` → „fără drept de scriere". Mesajul e mai vag decât realitatea. Verificarea `get diagnostics` rămâne ca plasă, dar nu se declanșează niciodată pe calea asta.
- (sarcina 6) dropdown-ul „către…" se închide la `onBlur` cu `setTimeout`, fără anulare la focus în meniu — navigarea cu Tab îl închide sub degete. Mouse-ul merge.
- (sarcina 6) atașamentele urcate și nescoase înainte de comutarea pe alt tichet rămân orfane în storage (fără `event_id`), iar nici destinatarul ales, nici atașamentele în așteptare nu marchează formularul murdar — deci nimic nu avertizează.
- (sarcina 6) `CLAUDE.md` afirmă că redimensionarea modal↔panou nu pierde ce ai scris. Recenzentul a stabilit din cod că e FALS și PREEXISTENT: `SheetHost` și `SplitView` randează `IssueForm` din două poziții diferite în arbore, deci trecerea pragului de 1200px demontează instanța. Fraza din documentație trebuie corectată la Sarcina 11.
- (sarcina 9) după cele 900ms, rândul nu doar își stinge bulina, ci SARE din „Necitite" în „Mai devreme" sub ochii tăi, cu panoul deschis. Întârzierea rezolvă „de ce era aprinsă", nu „unde mi-a fugit rândul". Ar cere înghețarea grupării cât timp un tichet e deschis — scop nou, nu îl deschid acum.
- (sarcina 9) dacă `Promise.all` din boot aruncă, `loadInbox` nu se mai cheamă și `inboxLoaded` rămâne fals pentru totdeauna — ecran blocat pe „Se încarcă…". Tipar preluat de la `loadDue`, deci preexistent.
- (sarcina 9) necititul arată altfel în sidebar (text estompat) față de bara de jos (bulină în accent). Consecvent cu vecinii în fiecare loc, inconsecvent între ele.
- (sarcina 9) `horizontal:last-project` se poluează cu slug-ul proiectului străin când o pasă docată îl încarcă. Inert azi, fiindcă bootul verifică `LAST_VIEW_KEY` înaintea ultimului proiect — dar devine bug dacă cineva schimbă ordinea verificărilor din boot.
- (sarcina 9) scurgerea de taste `C`/`O` pe „Pe mine" cu pasă docată n-are acoperire automată în `test:nav`; rămâne verificată doar manual.

## Bug preexistent, nu al acestui branch

`npm run test:nav` pica intermitent pe verificarea URL-ului dupa stergere.
Reprodus si pe `434a698` — varful lui master, inainte de orice cod al
functionalitatii — deci nu e introdus aici. Dispare sub orice instrumentare,
ceea ce arata o cursa reala, nediagnosticata inca. Merita un bilet separat.

Si: `scripts/test-nav.mjs` lasa un `vite` orfan pe portul 5211 cand `page.goto`
moare cu timeout necaptat — rularea urmatoare pica fals, fara legatura cu bug-ul.
